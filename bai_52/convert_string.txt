_ Phương thức chuyển đổi lớp String

+ concat() -> nối chuỗi

+ replace() -> thay thế chuỗi mới

+ toLowerCase() -> thay thành chuỗi in thường

+ toUpperCase() -> thay chuỗi in hoa

+ trim() -> xóa khoảng trắng ở đầu và cuối chuỗi

+ toCharArray() -> chuyển chuỗi thành mảng ký tự

+ substring() -> cắt chuỗi

Vd substring(3,5) -> lấy từ vị trí index 3 đến trước index 5
   substring(3) -> lấy từ vị trí index 3 đến hết chuỗi

+ split() -> phân tách chuỗi thành mảng
